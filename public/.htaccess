RewriteEngine On
RewriteBase /

# Serve existing files and folders as-is; send every other path to the SPA.
RewriteRule ^index\.html$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
